{
  "name": "tick-obsidian",
  "version": "0.6.0",
  "description": "Quick to-do capture and toggle on a local markdown file.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "node esbuild.config.mjs production",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "esbuild": "^0.20.0",
    "obsidian": "latest",
    "typescript": "^5.0.0",
    "vitest": "^4.1.5"
  }
}
